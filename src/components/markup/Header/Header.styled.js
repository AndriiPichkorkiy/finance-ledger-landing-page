import styled from "styled-components";

export const Container = styled.div`
  padding: 22px;
  color: ${(props) => props.theme.text.colorTextMain};
`;

export const Logo = styled.div`
  padding: 22px;
`;
